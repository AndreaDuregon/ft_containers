//
// Created by simone giovo on 30/05/2021.
//

#include "deque.hpp"

int main(){
    
    ft::deque<int> d(10, 2);
    exit(0);
    /* ft::listIterator<int*>    it(d.list->begin());
    size_t  k = 0;
    ft::DequeIterator<int> it2(d.begin());
    while (it != d.list->end())
    {
        for (size_t i = 0; i < 8 && k < 10; i++)
        {
            std::cout << "Node[" << i << "] " << it._curr->value[i] << std::endl;
            std::cout << "Node[" << it2._index_v << "] " << *it2 << " " << it2._list_iterator._curr->value << std::endl;
            k++;
            ++it2;
        }
        it++;
    }
    --it2;
    for (size_t i = 0; i < 10; i++)
    {
        std::cout << "RevNode[" << it2._index_v << "] " << *it2 << " " << it2._list_iterator._curr->value << std::endl;
        --it2;
    }
    std::cout << std::endl;
    std::cout << "||TEST COSTRUTTORE RANGE||" << std::endl;
    std::cout << std::endl;

    ft::DequeIterator<int> it3(d.end());
    ft::deque<int> d2(it2, it3); */
        
};