//
// Created by simone giovo on 30/05/2021.
//

//#include "deque.hpp"
#include "Ddeque.hpp"
#include <deque>

int main(){
/*
    ft::deque<int> d(10, 7);
    size_t  k = 0;
    ft::DequeIterator<int> it2(d.begin());

    while (it2 <= d.end())
    {
        std::cout << "Node[" << k << "] " << *it2 << std::endl;
        it2++;
        k++;
    }
    it2--;
    k--;
    while (it2 >= d.begin())
    {
        std::cout << "RevNode[" << k << "] " << *it2  << std::endl;
        --it2;
        k--;
    }
    std::cout << std::endl;
    std::cout << "||TEST COSTRUTTORE RANGE||" << std::endl;
    std::cout << std::endl;

    ft::DequeIterator<int> it3(d.end());
    ft::deque<int> d2(it2, it3); 
*/
    std::deque<int> test;
    ft::deque<int> test1;

    test1;
        
};